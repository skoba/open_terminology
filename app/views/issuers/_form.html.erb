<%= form_for(@issuer) do |f| %>
  <% if @issuer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issuer.errors.count, "error") %> prohibited this issuer from being saved:</h2>

      <ul>
      <% @issuer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vsab %><br />
    <%= f.text_field :vsab %>
  </div>
  <div class="field">
    <%= f.label :source %><br />
    <%= f.text_field :source %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
